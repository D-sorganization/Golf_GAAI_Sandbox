---
type: qa-report
id: E02S02
story: E02S02 — Add heavy-integration-tests.yml workflow for long-running tests
verdict: PASS
qa_cycles: 1
created_at: 2026-03-19
---

# E02S02 — QA Report

## Verdict: PASS

All 8 acceptance criteria met on first cycle. No remediation required.

## Per-Criterion Results

| AC | Description | Evidence | Result |
|----|-------------|----------|--------|
| AC1 | `.github/workflows/heavy-integration-tests.yml` created | File exists in worktree | PASS |
| AC2 | Triggers: `workflow_dispatch`, weekly Sunday 2am UTC, PR label `run-heavy-tests` | `on.workflow_dispatch`, `schedule: cron: "0 2 * * 0"`, `pull_request: types: [labeled]` + `if` condition on job | PASS |
| AC3 | `pytest -m "live_simulation or heavy_integration"` with 60 min job timeout | Line 93: `-m "live_simulation or heavy_integration"`, Line 46: `timeout-minutes: 60` | PASS |
| AC4 | `heavy_integration` marker registered in `pyproject.toml` | `"heavy_integration: marks tests as heavy integration tests (long-running, requires full engine stack)"` in markers list | PASS |
| AC5 | `pytest-results.xml` artifact via `actions/upload-artifact@v4` | `--junitxml=pytest-results.xml`, `uses: actions/upload-artifact@v4`, `path: pytest-results.xml` | PASS |
| AC6 | `CONTRIBUTING.md` one paragraph on when to trigger | "### Heavy Integration Tests" section with full explanatory paragraph | PASS |
| AC7 | `ci-standard.yml` excludes both markers | `-m "...and not live_simulation and not heavy_integration"` in Run Core Test Suite step | PASS |
| AC8 | YAML valid (`actionlint` check) | `yaml.safe_load` PASS; all 3 triggers, job structure, and expressions validated programmatically; `actionlint` not installed — structural validation confirmed | PASS |

## Rule Compliance

- No `print()` calls introduced in `src/`
- No `type: ignore` introduced
- No Python code changes (YAML + TOML + Markdown only) — TDD requirement not triggered
- `ruff check` not applicable (no Python source files modified)
- No secrets or credentials introduced
- No files >10MB added

## Memory Alignment Check

No new patterns introduced that conflict with `conventions.md`. Workflow structure follows established patterns from `nightly-cross-engine.yml`. No new architectural decisions required. No memory delta required.
