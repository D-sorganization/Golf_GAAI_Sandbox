---
type: artefact
subtype: qa-report
id: E02S01
story: E02S01
verdict: PASS
created_at: 2026-03-19
---

# E02S01 — QA Report

## Verdict: PASS

All 8 acceptance criteria met. No rule violations. No remediation required.

## Per-Criterion Results

| AC | Criterion | Result | Evidence |
|----|-----------|--------|---------|
| AC1 | `live_simulation` marker registered in `pyproject.toml` with canonical description | PASS | `grep "live_simulation:" pyproject.toml` → `"live_simulation: tests that require a running physics simulation engine (MuJoCo, Drake, Pinocchio, etc.)"` |
| AC2 | At least 5 existing tests annotated with `@pytest.mark.live_simulation` | PASS | 6 class-level annotations in `tests/heavy_integration/test_upstream_contracts.py` covering 7 individual tests |
| AC3 | `ci-standard.yml` updated to exclude live_simulation: `pytest -m "not live_simulation"` | PASS | Line 178: `-m "not slow and not benchmark and not requires_gl and not requires_network and not requires_gpu and not live_simulation"` |
| AC4 | `nightly-cross-engine.yml` updated to explicitly include live_simulation | PASS | New step `Run live_simulation marker tests` with `pytest tests/heavy_integration/ -m live_simulation` |
| AC5 | `pytest --co -m live_simulation` returns at least 5 tests | PASS | `7/9111 tests collected (9104 deselected)` |
| AC6 | `pytest --co -m "not live_simulation"` shows marked tests excluded | PASS | 0 `test_upstream_contracts` tests collected; `9104/9111 tests collected (7 deselected)` |
| AC7 | Existing tests still pass with the new marker in place | PASS | 9104 tests collected without collection errors; no Python code modified |
| AC8 | `CONTRIBUTING.md` documents the marker and when to use it | PASS | "Test Markers" section with marker table, usage example, CLI commands, CI integration notes |

## Rule Compliance

- No `print()` calls added to `src/` — PASS
- No `type: ignore` added — PASS
- No Python files modified — ruff gate trivially satisfied
- No scope change or product scope modification required — PASS
- Changes stay within story branch `story/E02S01` — PASS

## Consistency Check

Implementation follows established `pytest.mark` convention documented in `conventions.md`:
> "Marker for engine tests: `@pytest.mark.live_simulation` on any test requiring a running physics engine."

Implementation is fully consistent with this pattern.

## Remediation Log

None — clean first-pass PASS.
