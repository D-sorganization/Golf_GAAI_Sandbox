---
type: memory-delta
id: E02S02
story: E02S02 — Add heavy-integration-tests.yml workflow for long-running tests
created_at: 2026-03-19
requires_ingestion: false
---

# E02S02 — Memory Delta

## Assessment

No new durable patterns or conventions emerged from this delivery. The implementation followed established patterns from `nightly-cross-engine.yml` and existing GAAI delivery conventions.

## Potential Pattern Updates (Low Priority)

The PR label trigger pattern (`pull_request: types: [labeled]` + job-level `if` on `github.event.label.name`) is a repeatable pattern for opt-in CI workflows. If future stories add similar label-triggered workflows, this pattern should be documented in `conventions.md` under a CI/CD section.

## Recommendation

No immediate memory ingestion required. Flag for Discovery if a second label-triggered workflow is added — at that point, extracting the pattern to `conventions.md` becomes worthwhile.
