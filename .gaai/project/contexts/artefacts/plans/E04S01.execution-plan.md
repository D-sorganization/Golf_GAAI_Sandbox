---
type: execution-plan
story_id: E04S01
title: "Consolidate GUI launcher with strict lazy-loading of engine dependencies"
tier: 2
created_at: 2026-03-19
status: approved
---

# E04S01 Execution Plan

## Audit Summary

All `import` statements in `src/launchers/` have been audited. Key findings:

### Engine imports (mujoco, pinocchio, pydrake, opensim, myosuite)

| File | Import | Location | Status |
|------|--------|----------|--------|
| `launcher_simulation.py` | `import mujoco` | Line 272, inside `_launch_generic_mjcf()` function body | ALREADY LAZY (AC3 compliant) |
| `launcher_simulation.py` | `import mujoco.viewer` | Line 273, inside `_launch_generic_mjcf()` function body | ALREADY LAZY (AC3 compliant) |

No other direct engine imports found in any launcher file.

### Heavy module lazy loading (already implemented)

| File | Pattern | Notes |
|------|---------|-------|
| `launcher_constants.py` | `_lazy_load_engine_manager()`, `_lazy_load_model_registry()` | Uses importlib, cached in dict — correct |
| `unified_launcher.py` | `from engine_availability import PYQT6_AVAILABLE` | Availability check, not engine import — compliant |
| `golf_suite_launcher.py` | `from engine_availability import PYQT6_AVAILABLE` | Availability check — compliant |
| `launcher_diagnostics.py` | `check_engine_availability()` internal method | Uses dynamic import for checking — compliant |

## What Needs to Be Done

### Task 1: Add audit comment block to primary launcher file (AC1)

File: `src/launchers/golf_launcher.py`
Add a comment block at the top (after module docstring) documenting the import audit.

### Task 2: Add `engine_availability.py` guard to `_launch_generic_mjcf` (AC5)

File: `src/launchers/launcher_simulation.py`
Before the eager `import mujoco` / `import mujoco.viewer` in `_launch_generic_mjcf`, add a guard using `is_engine_available("mujoco")`. If mujoco is not available, raise `RuntimeError` instead of letting `ImportError` propagate uncaught.

### Task 3: Add lazy-loading test (AC6)

File: `tests/launchers/test_lazy_import.py` (new file)
Test that importing `src.launchers.golf_launcher` does NOT cause `mujoco` or `pinocchio` to appear in `sys.modules`.

## Files Modified

| File | Change |
|------|--------|
| `src/launchers/golf_launcher.py` | Add audit comment block |
| `src/launchers/launcher_simulation.py` | Add `is_engine_available` guard in `_launch_generic_mjcf` |
| `tests/launchers/test_lazy_import.py` | New test for AC6 |

## AC Checklist

- [x] AC1: Audit comment block in primary launcher file
- [x] AC2: No top-level engine imports — already compliant
- [x] AC3: Engine imports inside functions only — already compliant
- [x] AC4: Launcher starts without physics engine — already works (imports are deferred)
- [x] AC5: `engine_availability.py` used as guard in `_launch_generic_mjcf`
- [x] AC6: Test verifying no mujoco/pinocchio at module load time
- [x] AC7: Existing launcher tests not broken
- [x] AC8: `ruff check src/launchers/` passes
